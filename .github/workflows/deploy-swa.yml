name: Deploy to Azure Static Web Apps

on:
  workflow_dispatch:
  push:
    branches:
      - main
    tags:
      - "v*"
    paths:
      - '.github/workflows/deploy-swa.yml'
      - 'packages/**'
      - 'package.json'
      - 'package-lock.json'
      - 'scripts/**'
      - 'tsconfig.json'

permissions:
  contents: read

jobs:
  deploy:
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    name: Deploy to Azure Static Web Apps
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          submodules: true
          lfs: false

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build all workspaces
        run: npm run build

      - name: Verify API dist
        run: test -f packages/web/api/dist/functions/health.js || (echo "❌ API bundle not found at packages/web/api/dist/functions/" && exit 1)

      - name: Verify OTel externals present
        run: |
          # Regression guard for https://github.com/sabbour/kickstart/issues/1041
          # materialize-api-externals.mjs (postbuild) must have copied OTel packages
          # into packages/web/api/node_modules/ so they ship in the SWA deploy zip.
          # If this fails: the postbuild step did not run, or materialize script failed.
          test -d packages/web/api/node_modules/@azure/monitor-opentelemetry || \
            (echo "❌ @azure/monitor-opentelemetry not found in packages/web/api/node_modules/ — OTel externals were not materialized. This will cause MODULE_NOT_FOUND at runtime and 404 on all API endpoints. See https://github.com/sabbour/kickstart/issues/1041" && exit 1)

      - name: Deploy to Azure Static Web Apps
        id: swa_deploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "packages/web/dist"
          api_location: "packages/web/api"
          output_location: ""
          skip_app_build: true
          skip_api_build: true

      - name: Resolve smoke test target
        id: production_url
        run: |
          # SWA action sets static_web_app_url; use it instead of parameter file
          DEPLOYMENT_URL="${{ steps.swa_deploy.outputs.static_web_app_url }}"
          if [ -z "$DEPLOYMENT_URL" ]; then
            echo "⚠️ SWA deployment URL not available, skipping smoke test"
            echo "base_url=" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "base_url=${DEPLOYMENT_URL}" >> "$GITHUB_OUTPUT"

      - name: Smoke check live API health
        if: steps.production_url.outputs.base_url != ''
        env:
          SWA_HEALTHCHECK_URL: ${{ steps.production_url.outputs.base_url }}/api/health
        run: node scripts/check-swa-health.mjs
