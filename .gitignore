# Dependencies
node_modules/

# Build output
dist/
# packages/core/dist is generated by `tsc --build`; never commit it.
# The stale dist (pre-FSM-removal) shipped deleted symbols — keeping it
# gitignored prevents future drift. See issue #401.
packages/core/dist/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/settings.json
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Playwright
test-results/
playwright-report/

# Azure
.azure/
local.settings.json

# Squad: per-agent worktrees (checked out alongside the main working tree)
.worktrees/

# Squad: ignore runtime state (logs, inbox, sessions)
.squad/orchestration-log/
.squad/log/
.squad/decisions/inbox/
.squad/sessions/
# Squad: SubSquad activation file (local to this machine)
.squad-workstream
dp-46-body.md

# Squad: GitHub App identity (keys, config, tokens — never commit)
.squad/identity/keys/
.squad/identity/apps/
.squad/identity/config.json

# Squad: per-ceremony gh CLI config (isolates auth from human hosts.yml — #1087)
.squad/runtime/

# Squad: local session artifacts (may contain captured agent output — #1087 C/R5)
events.jsonl
.copilot/session-state/
.squad-*-token
.squad-*-token.json
