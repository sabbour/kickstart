---
"@aks-kickstart/pack-azure": minor
---

Add `azure.quota_lookup` tool that queries Azure Compute quota for a given SKU family in a subscription and region. Returns available units, total limit, current usage, utilization percentage, and a portal URL to request a quota increase. Enables GPU SKU quota pre-flight checks before deployment recommendations.
