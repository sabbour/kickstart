---
"@aks-kickstart/pack-core": patch
---

Codesmith agent now has access to the reviewer via optional mid-generation asTools consultation (`ask_core_reviewer`, max 3 turns) for early feedback on complex files. The deterministic post-generation review gate remains enforced by the harness independently.
