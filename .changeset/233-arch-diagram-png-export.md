---
"@aks-kickstart/web": minor
---

ArchitectureDiagram component now includes an "Export PNG" button. Clicking it captures the rendered Mermaid SVG as a PNG using browser-native canvas (no server round-trip, no external dependencies) and triggers a download — ready for PowerPoint and offline review decks.
