---
"@aks-kickstart/web": patch
---

Phase-C codesmith-progress E2E test now properly verifies that the GenerationProgress surface contains a root component, catching regressions where the surface exists but is empty.
