---
"@aks-kickstart/pack-aks-automatic": minor
---

The AKS manifests author now generates KEDA scaler configs per workload shape, respects AKS Automatic mutating webhooks (won't duplicate auto-injected sidecars), and includes Workload Identity annotations by default.
