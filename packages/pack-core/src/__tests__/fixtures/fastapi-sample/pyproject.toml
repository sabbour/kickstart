[tool.poetry]
name = "fastapi-sample"
version = "0.1.0"
description = ""

[tool.poetry.dependencies]
python = ">=3.11"
fastapi = "^0.110.0"
sqlalchemy = "^2.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
