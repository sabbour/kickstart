# Leela DP Review — #488 v2 Step 13: Docs + Cleanup

**Date:** 2026-05-28
**Issue:** #488
**Verdict:** APPROVE_WITH_CONDITIONS
**Label applied:** `leela:approved-dp`

---

## Conditions (all blocking, all addressable without architectural rework)

### C1 — Add 7 `docs/` redirect stubs to Phase A DELETE list
Files `docs/fsm.md`, `docs/prompt-architecture.md`, `docs/extending-components.md`, `docs/mcp-server.md`, `docs/development.md`, `docs/a2ui-catalog.md`, `docs/deployment.md` are redirect stubs pointing at docs-site pages being deleted/rewritten. Not in the DP inventory. Must be deleted in Phase A.

### C2 — Add `DEVELOPMENT.md` (root) to Phase D update list
Contains live `STEPWISE_GENERATION_V1` reference and v1 env vars. Add to Phase D. Add `STEPWISE_GENERATION_V1` to the Phase E grep acceptance pattern.

### C3 — Add `SUMMARY.md` to Phase A DELETE list
Companion artifact to `HERMES-271-TEST-PLAN.md` / `QUALITY-DECISION-271.md`. Orphaned temp doc. Delete in Phase A.

### C4 — Remove "Update Docusaurus sidebar config" from Phases A and C
`docs-site/sidebars.ts` uses `{type: 'autogenerated', dirName: '.'}`. No manual updates needed when files are deleted. Striking this step removes dead work and prevents confusion.

### C5 — `harness-api-reference.md` must reproduce §16 naming convention table; brief gets archive header
The brief's section 16 (wire conventions: tool `.`, user action `:`, component `/`, etc.) is the canonical source. The new `docs/harness-api-reference.md` must reproduce it verbatim, not defer to the brief. After extraction, `docs/v2-implementation-brief.md` should get an archive header pointing to the new reference doc.

### C6 — Add sequencing gate to acceptance criteria
Several Steps 4.x–8.x are still open. Step 13 must not execute until all Steps 1–12 (#476–#487) are merged to `main`. Add this as an explicit acceptance criterion.

---

## Core approach: Approved

- Delete-first strategy is correct.
- v2 terminology (harness+packs, primitives, registries) is architecturally accurate.
- Phase structure (A→B→C→D→E) is sound.
- CHANGELOG entry is the right artifact (no migration guide needed — v1 had no upgrade path).
- Scribe's CHANGELOG grouping (Added/Changed/Removed) is correct.
- `docs/v2-implementation-brief.md` staying as historical record (with archive header) is the right call.
